{
  "name": "opencode-wechat",
  "version": "0.1.0",
  "description": "Bridge WeChat ClawBot messages into OpenCode sessions via ilink API",
  "type": "module",
  "bin": {
    "opencode-wechat": "./cli.mjs"
  },
  "files": [
    "cli.mjs",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build opencode-wechat.ts --target node --outfile dist/opencode-wechat.js && bun build setup.ts --target node --outfile dist/setup.js",
    "start": "node cli.mjs start"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": [
    "opencode",
    "wechat",
    "bridge",
    "ilink",
    "ai-agent"
  ]
}
